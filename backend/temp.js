// Define a simple function that adds two numbers
function addTwoNumbers() {
  const num1 = 10;
  const num2 = 20;
  const sum = num1 + num2;
  
  // The sum is calculated but not displayed
  return sum;
}

// Call the function
const result = addTwoNumbers();

console.log(result);